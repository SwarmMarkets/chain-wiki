import styled from 'styled-components';

const Container = styled.div`
  width: 100%;
  max-width: 390px; // Max width for iPhone 12
  margin: 0 auto;
  padding: 0 15px;
  box-sizing: border-box; // Ensures padding doesn't affect overall width
`;

export default Container;
