export interface IpfsProjectContent {
  name: string
  address: string
  htmlContent: string
}

export interface IpfsArticleContent {
  name: string
  address: string
  tokenId: string
  htmlContent: string
}